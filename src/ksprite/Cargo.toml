[package]
name = "ksprite"
version = "0.1.0"
edition = "2024"

[dependencies]
kcore = { path = "../kcore" }
kmath = { path = "../kmath" }
kmesh = { path = "../kmesh" }
ktexture = { path = "../ktexture" }
bytemuck = { version = "1.25.2", features = ["derive"] }

[dev-dependencies]
# 测试里解析 WGSL，核对实例布局与着色器入口。
naga = { version = "30", features = ["wgsl-in"] }
