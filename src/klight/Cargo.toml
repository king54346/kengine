[package]
name = "klight"
version = "0.1.0"
edition = "2024"

[dependencies]
kmath = { path = "../kmath" }
# 光源要打包成 GPU uniform 数组。
bytemuck = { version = "1.25.2", features = ["derive"] }

[dev-dependencies]
kshader = { path = "../kshader" }
