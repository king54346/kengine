[package]
name = "kmesh"
version = "0.1.0"
edition = "2024"

[dependencies]
kasset = { path = "../kasset" }
kcore = { path = "../kcore" }
kmath = { path = "../kmath" }
# 顶点需要能按位重解释成字节以直接上传显存。
bytemuck = { version = "1.25.2", features = ["derive"] }
