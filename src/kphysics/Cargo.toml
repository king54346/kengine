[package]
name = "kphysics"
version = "0.1.0"
edition = "2024"

[dependencies]
kcore = { path = "../kcore" }
kmath = { path = "../kmath" }
kmesh = { path = "../kmesh" }
# debug-render：把碰撞体/关节/接触点拆成线段，只要求一个「画线」回调。
rapier3d = { version = "0.35", features = ["debug-render"] }
