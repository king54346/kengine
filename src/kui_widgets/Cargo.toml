[package]
name = "kui_widgets"
version = "0.1.0"
edition = "2024"

[dependencies]
# 控件建在核心之上：布局、绘制图元、命中测试都来自那一层。
kui = { path = "../kui" }
kfont = { path = "../kfont" }
kmath = { path = "../kmath" }
# 贴图控件只需要 `Uuid`：控件层不认识 `ktexture::Texture`，
# 它拿到的就是一个 id 和一块 UV。
kcore = { path = "../kcore" }
