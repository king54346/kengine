[package]
name = "kgizmo"
version = "0.1.0"
edition = "2024"

[dependencies]
kmath = { path = "../kmath" }
bytemuck = { version = "1.25.2", features = ["derive"] }

[dev-dependencies]
# 测试里用它核对 WGSL 的顶点布局与 CPU 侧结构体一致。
naga = { version = "30", features = ["wgsl-in"] }
